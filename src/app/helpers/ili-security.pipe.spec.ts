import { IliSecurityPipe } from './ili-security.pipe';

describe('IliSecurityPipe', () => {
  it('create an instance', () => {
    const pipe = new IliSecurityPipe();
    expect(pipe).toBeTruthy();
  });
});
